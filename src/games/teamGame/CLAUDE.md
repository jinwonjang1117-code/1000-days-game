# Co-op Roguelike — Project Context

This file is persistent context for Claude Code. Read this before starting any task on the co-op roguelike. This game shares a repo with an existing platformer (Hungry Monster clone) — do not modify the platformer's files unless a task explicitly says to touch the shared main menu.

**Full design detail lives in [DESIGN.md](DESIGN.md)** (same directory) — the design doc, with the complete role/item/enemy/combo tables, run structure, and sprite checklist. This file is a condensed summary plus the engineering-specific decisions DESIGN.md doesn't cover (networking internals, build phases, working conventions). When the two disagree, DESIGN.md is the source of truth for game design; this file is the source of truth for how it's actually built so far.

## What this game is

A 2-player online co-op roguelike, shared-screen/shared-room (both players always in the same room together, not separate screens), inspired by The Binding of Isaac. One player hosts a room and shares a short room code; the other joins with that code. Runs are randomized and replayable: randomized item drops, randomized room enemy composition, seven selectable "roles" (elements) with mechanical synergies between them. Also playable single-player (`PlayScene.ts`) — single-player has always been "the same host-authoritative simulation with no joiner," so it's not a separate mode built twice; see the `GameSimulation` build-status entry below for the split.

## Architecture decisions (settled — don't relitigate without asking)

- **Stack:** Vite + TypeScript + Phaser 3, same repo as the platformer, routed to via the existing main menu scene.
- **Physics:** Arcade Physics, gravity disabled (`{ x: 0, y: 0 }`) — this is a top-down game, not a platformer. 8-directional movement via velocity, with diagonal movement normalized so it isn't faster than cardinal movement.
- **Camera:** Static per room. The camera never scrolls or follows a player within a room — it only cuts/transitions between rooms. This is a deliberate Isaac-style choice and also sidesteps multiplayer camera-follow conflicts.
- **Networking:** PeerJS (WebRTC), host-authoritative. The host's game instance is the source of truth: it simulates both players, all enemies, and resolves all hits/collisions, and broadcasts authoritative state. The joiner sends only input (movement direction, attack triggers) to the host — the joiner never simulates their own movement locally; they render based on interpolated state received from the host, never local prediction.
- **Room code flow:** Host generates a short alphanumeric PeerJS id, displays it; joiner enters it to connect. Handle failed/nonexistent codes with a visible error, not a silent hang.
- **Pause:** Either player can pause (e.g. Esc) — pauses for both, opens a menu on both screens. Matters more than it sounds: single-sitting runs of 70-90 rooms (see Run Structure) need a real pause. Built — see the build-status entry below. Host-authoritative like everything else: the joiner's Esc is a request (`PauseToggleMessage`), only the host actually flips `isPaused` and freezes the world (`this.physics.pause()`), same as the platformer's `GameScene.ts` already does for its own freeze moments.
- Networking is layered UNDER regular gameplay code, not bolted on after. Player movement, attacks, and room logic should be written so they don't care whether input came from a local keypress or a forwarded network message — same input-handling code path either way.

## Lives & death (settled — DESIGN.md §3)

No HP pool — players have a direct life count, any hit removes exactly one life (no damage variance), with ~1-1.5s of post-hit invincibility (visual flicker) so overlapping hits can't delete multiple lives at once. Lives are per-player, not shared. Starting lives: 5. Running out takes you out for the rest of the current level (input ignored, sprite grayed out); you respawn at the start of the next level with a **fixed 3 lives** (not the starting 5, not inherited from your teammate — this was previously TBD, now settled). If both players are out within the same level, the run ends. Rare life-item drops on room-clear grant +1.

Prototyped ahead of real Player/enemy content in `DevTestScene.ts`:
- `src/games/teamGame/gameplay/lives.ts` — host-only pure state (`LifeState`, `applyHit`, `respawnForNextLevel`). The joiner never runs this, only renders what the host broadcasts.
- `src/games/teamGame/gameplay/flicker.ts` — reusable invincibility-flicker visual, driven locally on each client (not synced frame-by-frame — only the `isInvincible` boolean travels over the network).
- `respawnForNextLevel` exists but isn't wired to any trigger yet — there's no real level-clear event until Phase F+ builds actual rooms/floors.
- The debug enemy + Arcade overlap detection in `DevTestScene.ts` is the same collision pattern real enemies will reuse later, not a one-off hack.

## Attack system (settled — DESIGN.md §4)

Three distinct attack-input shapes, branching by equipped role — **architect this as three separate input handlers from the start**, not retrofitted onto one:

| Input pattern | Roles |
|---|---|
| Space → fire projectile toward the direction you're facing | Ice, Glue, Poison, Electric, Gravity |
| Space → instant line/beam in the direction you're facing, hits all enemies in path | Laser |
| Hold Space to charge → release to fire; charge duration sets travel speed | Bomb |

Every character has this baseline "default attack" (tears-style) before any role is equipped — a role changes *what the attack does and how it's fired*, not whether an attack exists. Aim is keyboard-driven (whichever way you're currently moving, or last moved) — no mouse involved anywhere in this game; settled, previously "click → fire toward cursor."

Only the first row (fire the default projectile) is built so far — see the build-status entries below, including the room-clear items that now modify this same default projectile (damage/speed/range/size multipliers, Multi Shot, Multi Direction, Piercing, Homing) and its actual location today (`simulation/GameSimulation.ts`'s `tryFirePlayer`, not `DevTestScene.ts` — see the `GameSimulation` extraction entry). Laser and Bomb wait for the role system to exist, since nothing can select them yet. `src/games/teamGame/gameplay/attack.ts` + `entities/Projectile.ts` are where the eventual branch point for the other two input shapes will go. **Known gap, checked when `GameSimulation` was extracted**: fire input is currently a single `firing: boolean` (`tryFirePlayer`'s param) — fine for the projectile/beam rows above, but Bomb's hold-to-charge input needs something richer (charge duration, not just held/not-held). That'll be a contained signature change to `GameSimulation`'s input method when Bomb lands, not a redesign — flagging now so it isn't a surprise later.

## The seven roles (settled — DESIGN.md §5)

Ice, Glue, Poison, Electric, Gravity, Laser, Bomb. Fire was considered and removed. Full effect/attack-shape/notes table in DESIGN.md — the one non-obvious one: **Bomb costs a full life** (not partial damage) if the player or teammate is caught in the blast, and only Electric can detonate a live bomb early.

### Role-change rule

- First role pick: free, near the start of the run.
- One additional free role change: offered specifically after clearing level 1 (not before, not automatically available earlier).
- Further changes: available via coins at a shop (shop system not yet designed/built — deferred, see below).
- Boost items that are role-specific becoming "orphaned"/useless after a role change is intentional — do not build any system to prevent, refund, or migrate stats on role change. This is a deliberate risk/cost of switching.

## Synergy / combo system (settled — DESIGN.md §6)

Status effects are shared world-state — either player's ability can trigger a combo off a status the *other* player applied, not just their own. A handful of marquee combos are designed (see DESIGN.md table, e.g. Ice+Electric = shatter, Gravity+Laser = cluster-then-sweep, Electric+Bomb = early detonation — the **only** role allowed to interact with Bomb's fuse). Not all 21 role pairs need a bespoke effect — most just fall back to "naturally synergistic," and that's fine.

## Item system (settled — DESIGN.md §7)

- **Default attack** — baseline, always present, not randomized.
- **Role items** — define/change a player's role. Scheduled drops at the end of levels 1, 2, 4, 6, 8 — only 5 opportunities across 7 roles, so no single run shows every role (intentional, for replayability).
- **Boost items** — role-agnostic stat ups (attack speed, move speed, damage, etc.) plus role-specific modifiers (e.g. "+15% freeze chance") in the same tier. Regular room-clear reward. Role-agnostic half built — see build status below; role-specific modifiers wait on the role system.
- **Holdable item** — a single equip slot, not a stacking collection. A new pickup auto-swaps and drops the old one on the ground (teammate can grab the discard). Each role has one signature holdable (DESIGN.md table); several universal/co-op holdables also compete for the same slot (self-immunity, teammate-immunity, Second Wind, etc.). Not built — waits on the role system.
- **Life items** — rare, unscheduled room-clear drop, +1 life. Built — see build status below.
- **Boss-tier items** (engineering addition, not yet in DESIGN.md's table) — Multi Shot, Piercing, Homing, and Multi Direction, dropped specifically at boss-room clear rather than the regular pool. Unlike everything above, these are visually identified (not mystery) — an earned, known reward after a real fight. Stack (picking the same one twice compounds it) rather than being a one-time flag — see the build-status entry below.

## Run structure (settled — DESIGN.md §8-9)

10 levels, 6-10 rooms each, mini-boss ending levels 1-9, final boss (with role-favoring phases) on level 10 with no regular rooms. Role item drops at the end of levels 1, 2, 4, 6, 8. Five enemy archetypes (Swarmer, Tank, Chaser, Ranged shooter, Splitter) introduced on a schedule across levels 1-9. Room archetype mixing ramps up with floor depth (settled, implemented in `rooms/floorGenerator.ts`): single-archetype only below level 4, then a ramping chance to mix 2 archetypes from level 4, plus a smaller chance to mix 3 from level 8. Single-sitting only — no save/resume across sessions.

## Characters

- **Player 1:** reuses the existing wizard character (already designed/sprited elsewhere).
- **Player 2:** needs a new, visually distinct design — different silhouette/color language, so both players are readable at a glance during shared-screen combat with friendly-fire risk (relevant to Bomb).

## Explicitly deferred (not designed yet, don't build)

- Shop system (currency spending, role-change-for-coins)
- Local same-computer co-op mode (remote is being built first)
- Save/resume across sessions (explicitly rejected — single sitting only)

## Current build status

Networking-first build order (deliberately built before single-player game content, so the hardest architectural piece — sync — is proven against trivial content first):

- ✅ Phase A — Main menu wired to route into this game's scene(s)
- ✅ Phase B — PeerJS host/join lobby with room codes, connection confirmed end-to-end
- ✅ Phase C — Minimal synced movement proof-of-concept: two debug squares, host-authoritative, joiner input forwarded to host, host broadcasts positions ~20/sec. Interpolation deliberately NOT added yet at this stage (raw latency was intentionally left visible to establish a baseline).
- ✅ Phase D — Joiner-side interpolation: both squares ease toward the latest received position each frame (frame-rate-independent exponential smoothing, `INTERPOLATION_RATE` in `DevTestScene.ts`) instead of snapping, except the very first update after connecting (snaps immediately so nothing slides in from spawn). Host-side rendering untouched — it's already smooth via local simulation.
- ✅ Lives system (DESIGN.md §3) — prototyped on the same debug squares ahead of real Phase E/F+ content: per-player lives, post-hit invincibility with flicker, out-of-lives freeze/gray-out, a bouncing debug enemy + Arcade overlap standing in for real hit detection, all riding in the same `StateMessage` broadcast as position. See "Lives & death" above for the module breakdown. Not a numbered phase of its own — it's built across whatever the current phase is.
- 🟡 Phase E (sprite-free slice done) — `src/games/teamGame/entities/Player.ts` is a real entity now (still a colored `Rectangle`, no texture yet): movement, life state, invincibility flicker, and gray-out all live on it instead of duplicated host/joiner fields in `DevTestScene.ts`. Diagonal movement is now normalized (`setVelocityFromKeys`), closing the gap noted below in earlier phases. Same `simulated: boolean` split as before (host owns physics + `LifeState` per player; joiner renders received `PlayerState` and interpolates). Still outstanding for Phase E to be fully done: real sprites/art once available — nothing else about the entity should need to change when that lands.
- ✅ Default attack, fire-the-projectile only (DESIGN.md §4) — prototyped ahead of the numbered phases, same as lives. Space fires toward whichever direction the player is currently facing (`Player.getFacingAngle()`, derived from the same movement `KeyState` already used for velocity — no separate aim input exists), host-authoritative fire-rate (`gameplay/attack.ts`, mirrors `gameplay/lives.ts`'s pure-state style; `Player.tryFire`), host-simulated `Projectile` entities (`entities/Projectile.ts`, same `simulated: boolean` split as `Player`) that despawn past a max range rather than colliding with world bounds. Joiner never picks whether a shot fires — it just reports its held/not-held fire state as one more field on the regular `InputMessage` (`fire: boolean`, sent on change same as movement) and lets the host's cooldown decide; there's no separate `AttackMessage`/resend-timer, since aim has no continuous component to poll now that it's derived from discrete key state. Never hits either player (only Bomb has friendly-fire per DESIGN.md). `StateMessage.projectiles` is the protocol's first dynamic-length array field — real enemies will follow the same pattern. Laser and Bomb (the other two input shapes) are NOT built — nothing exists yet to select a role, so there'd be nothing to trigger them.
- ✅ Enemy health & death — enemies are a real `entities/Enemy.ts` (same `simulated: boolean` split as `Player`/`Projectile`): 3 HP, flashes and ticks down on each hit. Superseded by the room work below: death no longer respawns the same enemy — a killed enemy is just removed from its room's list for good, since "room clear" (the list is empty) is now the real mechanic that used to be stood in for by a respawn timer.
- ✅ Fixed room layout + transitions (step 1 of 3: fixed layout → real archetypes → procedural generation) — `rooms/floorLayout.ts` is a small hand-authored 2x2 room grid (`TEST_FLOOR`), shared compile-time knowledge on both host and joiner so only the *current* room coord needs to go over the wire (`StateMessage.roomCoord`), not the layout itself. A room now holds a *list* of enemies (`roomEnemies: Map<number, Enemy>`, `StateMessage.enemies: EnemyState[]` — enemies gained an `id` the same reason `Projectile` has one), and doors (4 fixed screen-edge zones, no physics body — checked by position, not Arcade overlap, since they're not obstacles) open once that list is empty. Walking into an open door is host-authoritative: `loadRoom()` tears down the current room's enemies/colliders and every live projectile (they don't carry through a door), teleports both players (`Player.teleport()`, new) to an entry point on the far side, and spawns the next room. On the joiner, a `roomCoord` change first calls `Player.resetInterpolation()` (new) on both players so the transition is a cut, not a slide, then the existing create/destroy-on-presence reconciliation (already used for projectiles) handles the enemy-list churn with no special-casing. Deliberately simplified for this first slice: closed doors don't block movement (no wall-collision system exists), no special room types (item/shop/mini-boss) yet.
- ✅ Solo test mode — `LobbyScene.ts`'s "솔로 테스트 (개발용)" button starts `DevTestScene` with `{ solo: true }`, skipping `peerConnection.ts` entirely (no `Peer` ever created). Reuses the host's simulation code path as-is (`role` stays `'host'`; a separate `isSolo` flag only relaxes the top-level connection guard) with no `joinerPlayer` at all — not an idle stand-in, genuinely absent. Two spots needed auditing because they used to assume both players exist together: room-enemy collider registration (now two independent `if (this.hostPlayer)`/`if (this.joinerPlayer)` checks) and the game-over check in `handleHit` (a missing joiner now counts as vacuously already-out, so dying alone correctly ends a solo run). **Superseded** — see the `GameSimulation`/`PlayScene` entry near the end of this list: `isSolo`/`setupSolo()` are gone from `DevTestScene.ts` entirely, single-player is now `PlayScene.ts`, and the two audited spots above live inside `GameSimulation` now (unaffected by the move).
- ✅ Networked pause (DESIGN.md §2) — Esc or a "계속하기" button in `DevTestScene.ts`; joiner's press is a `PauseToggleMessage` request, only the host flips `isPaused` and calls `this.physics.pause()`/`.resume()` (freezes both players, every enemy, and every live projectile in one call — nothing needed individual pause-awareness). `StateMessage.isPaused` broadcasts it, mirrored the same way `isGameOver` already is. All per-frame gameplay logic (movement, firing, sending, interpolation) short-circuits on `isPaused` for both roles; UI buttons keep working since they're event-driven, not gated by `update()`. The always-visible music/sfx toggle buttons were removed from teamGame (`LobbyScene.ts` and `DevTestScene.ts` — hub and the platformer's `StartScene` still have theirs, untouched) and now live inside this pause menu instead; `ui/audioToggles.ts`'s `createAudioToggleButtons` gained an optional position param to support that without changing its other call sites.
- ✅ Step 2 — real enemy archetypes (step 2 of 3: fixed layout → real archetypes → procedural generation) — `gameplay/enemyArchetypes.ts` (new, pure data, no Phaser dependency, mirrors `lives.ts`/`attack.ts`) defines the five from DESIGN.md §9 (Swarmer, Tank, Chaser, Ranged shooter, Splitter) with first-pass stats/movement/color, since roles don't exist yet to inform real matchup tuning. `entities/Enemy.ts` is now archetype-driven — constructor takes an `EnemyArchetype` instead of fixed module constants, and gained two host-only per-frame methods: `updateMovement(nearestPlayerPos)` ('bounce' is a no-op, Arcade physics already owns it from spawn; 'chase' beelines; 'keepDistance' backs off within half its firing range, else holds) and `tryFireAt(nearestPlayerPos, now)` (Ranged shooter only, reuses `gameplay/attack.ts`'s cooldown functions — confirmed generic, not Player-specific). Splitter spawns 2 Swarmers on death instead of just disappearing (children are Swarmers specifically, not more Splitters, so this can't recurse). `net/syncProtocol.ts`'s `EnemyState` gained an `archetype` field (always sent, not inferred) and `StateMessage` gained a separate `enemyProjectiles: ProjectileState[]` array for enemy-fired shots — kept as its own field rather than folded into `projectiles` with an owner tag, matching how enemies/projectiles/pause/game-over are already each their own top-level concern. `entities/Projectile.ts` gained an optional `color` (enemy shots render in a different color from player shots). `DevTestScene.ts` gained a parallel enemy-projectile subsystem (`enemyProjectiles`/`enemyProjectileColliders` Maps, `spawnEnemyProjectile`/`destroyEnemyProjectile`/`handleEnemyProjectileHitPlayer`, overlap registered against players instead of enemies — reuses the existing `handleHit`, no new damage path) and a `getNearestPlayerPos` helper that excludes players who are `isOut`. `rooms/floorLayout.ts`'s `TEST_FLOOR` is now a straight 6-room line — an empty start room plus one dedicated room per archetype, so each can be tested in isolation; `RoomDefinition.enemyCount: number` became `enemies: { archetype; count }[]`. Deliberately simplified for this slice: each test room is single-archetype on purpose — the mixing ramp in "Run structure" above only applies once step 3's real procedural generation exists to tune mix frequency against.
- ✅ Weak/Strong archetype tiers (mid-build addition, after step 3) — each of the 5 regular categories now comes in two tiers of the same behavior instead of one fixed stat block. `gameplay/enemyArchetypes.ts`'s `ArchetypeId` is now a template literal type (`` `${ArchetypeCategory}${'Weak'|'Strong'}` | 'boss' ``); a private `BASE_STATS` table (today's old per-category numbers, unchanged) plus a `TIER_SCALE` table and a `buildArchetype(category, tier)` generator produce all 10 entries, so tuning a category only means touching `BASE_STATS` once. Both tiers share a color — `entities/Enemy.ts` already renders a live "HP N" label above every enemy, which is the tell; size (±15%) is the other visible cue. Two per-category overrides on top of the generic output: **Strong Swarmer explodes on death** (`explodesOnDeath`/`explosionRadius` on `EnemyArchetype`, checked in `DevTestScene.ts`'s `handleProjectileHitEnemy` — hits any player within radius via the existing `handleHit`, no new damage machinery, so invincibility frames apply for free; `spawnExplosionEffect` is a purely cosmetic expanding-ring tween, triggered locally by both host's death handler and the joiner's `reconcileRoomEnemies` noticing the enemy vanish, same "notice it happened, react locally" pattern item pickups already use) and **Strong Splitter splits into 2 Weak Chasers** instead of the generic tier-matched 2 Strong Swarmers (fast-and-real rather than squishy fodder, without stacking two full Strong Chasers from one kill; Weak Splitter keeps the default Weak-Swarmer split). `rooms/floorGenerator.ts`'s old `unlockedArchetypes` became `unlockedCategories` + a `pickTier(category, level)`: Weak for a category's unlock level and the one after, then Strong at a 50% coin-flip once eligible, ramping `+10%`/level to an 80% cap — capped by `MAX_RAMP_LEVEL = 9` same as everything else, so late-unlocking categories (Tank unlocks 5, Splitter unlocks 7) never actually reach the 80% cap before the level ramp itself caps out.
- ✅ Step 3 — procedural floor generation (step 3 of 3: fixed layout → real archetypes → procedural generation) — `rooms/floorGenerator.ts` (new) replaces the old hand-authored `TEST_FLOOR` with a random-walk-generated floor built fresh per level (`generateFloor(level)`): starting from the empty start room at `{0,0}`, repeatedly extends from a random already-placed room into a random open neighboring cell until the level's room count is reached (`6` at level 1 ramping to `10` at level 9, per DESIGN.md §8's "6-10 rooms"); two rooms are connected iff adjacent-and-both-exist, the same adjacency-implies-door model `rooms/floorLayout.ts`'s `hasNeighbor` already used, so no separate edge list was needed. `rooms/floorLayout.ts` itself is now generic over *any* room list (`getRoomDefinition`/`hasNeighbor` take the list as a parameter) instead of reading one static module constant — the old `TEST_FLOOR`/`START_COORD` exports are gone.
  - **Difficulty ramp**: `unlockedArchetypes(level)` follows DESIGN.md §8's existing introduction schedule unchanged (swarmer+chaser from level 1, +rangedShooter at 3, +tank at 5, +splitter at 7). Room archetype mixing is a **new settled rule, superseding the old "levels 7-8 mixed, level 9 full mix" draft**: single-archetype only below level 4, then a ramping chance (`clamp((level-3)*0.2, 0, 0.8)`) to mix 2 archetypes, plus a smaller chance from level 8 to mix 3. Enemy count per room also scales with level. Everything is clamped at level 9's ramp for any level beyond that — level 10's "final boss only, no regular rooms" stays out of scope until a real final boss exists.
  - **Boss room**: BFS from the start room picks whichever placed room is furthest away as the boss room (guarantees it's deep in the floor); its `RoomDefinition.enemies` is set directly to a single `'boss'` archetype (new entry in `gameplay/enemyArchetypes.ts` — big, high-HP, plain melee chaser, no ranged/split, no per-level scaling yet), so it spawns through the *exact same* `spawnRoomEnemies` path as any other room, no special-casing needed there. What *is* boss-room-specific, in `DevTestScene.ts`: `isCurrentRoomBoss()` (looks up the current room in `floorRoomEntries`) makes `updateDoorVisuals()` hide the 4 directional doors and show a "hole" graphic instead once the room is clear, and `checkRoomTransition()` checks distance-to-hole instead of the door zones — touching it calls the new `startLevel(currentLevel + 1)` instead of `loadRoom()`, which generates a fresh floor, re-teleports both players to their normal spawn points (a level transition has no `enteredFrom` edge to reposition them otherwise), and rebroadcasts. This is explicitly a placeholder ("no need to implement the actual boss yet") — no phases, no special attacks, just a tougher body to give the win condition something real.
  - **Minimap**: `ui/MiniMap.ts` (new — first game-specific UI file, alongside `entities/`/`gameplay/`/`net/`/`rooms/`/`scenes/`), anchored top-right. Genuine fog-of-war: a room square stays invisible until it's either been visited or is adjacent to a visited one ("known"); the boss room renders in a distinct color as soon as it's known, same reveal timing as any other room. Driven by `refresh(exploredRooms, currentRoomCoord)`, called on every room change.
  - **Protocol**: `net/syncProtocol.ts` gained `LevelStartMessage` (new — the protocol's first one-off "structural" message, sent once per level start rather than polled/broadcast, mirroring `PauseToggleMessage`'s discrete shape) carrying the level number, start coord, and a lightweight `{coord, isBoss}[]` room list — the joiner never needs enemy composition here since enemy instances already arrive via the existing per-room `EnemyState` reconciliation. `StateMessage` gained `exploredRooms: RoomCoord[]`, host-accumulated and mirrored wholesale by the joiner each tick, same pattern as `isPaused`/`isGameOver`.
  - `floorRoomEntries` (a lightweight `{coord, isBoss}[]`) is shared scene state populated by both roles from whichever source they have — host from the just-generated `GeneratedFloor`, joiner from the received `LevelStartMessage` — so `hasNeighbor`/door logic/boss lookup/minimap all read from one place regardless of role.
- ✅ Room-clear item rewards (DESIGN.md §7) — built ahead of the role system, same as lives/attack were: only what doesn't need roles to make sense (role items and holdables stay deferred). `gameplay/items.ts` (new, pure data) defines a per-player `PlayerStats` record — `moveSpeedMultiplier` plus a `potato`-prefixed group (`potatoFireRateMultiplier`, `potatoDamage`, `potatoSpeedMultiplier`, `potatoRangeMultiplier`, `potatoSizeMultiplier` — every shot-related stat, renamed from `projectile*` mid-build), `invincibilityBonusMs`, and `hasMultiShot`/`hasPiercing` flags — and two item pools: `BOOST_ITEMS` (7 stat boosts + `'fart'`, a no-op joke item, dropped on every regular room clear) and `STRONG_ITEMS` (`multiShot`/`pierce` — a boss-exclusive tier, dropped only at boss-room clear). Heart (+1 life) isn't a stat mutator, so it's handled as a special case wherever an item's effect is applied, alongside a new `gameplay/lives.ts` `grantLife`. **Mystery pickups**: every regular-tier pickup (boost/heart/fart) renders identically (`entities/ItemPickup.ts`, a "?" visual) so the effect is unknown until consumed; only `STRONG_ITEMS` are visually identified. `entities/Player.ts` reads its own `PlayerStats` at every stat use site (`setVelocityFromKeys`, `tryFire`'s fire-rate check, `applyHit`'s invincibility duration) via new `getStats()`/`applyItem()`/`grantLife()` methods. `entities/Projectile.ts` gained per-shot `radius`/`damage`/`range`/`pierceCount` options (previously fixed module constants) so a boosted player's shots actually differ; the 3 base constants are now exported so `DevTestScene.ts` can compute `base * multiplier` without duplicating them. Item ids are named after the stat they touch (`speed`, `fireRate`, `damage`, `projectileSpeed`, `range`, `size`, `invincibility`, plus boss-tier `multiShot`/`pierce`) rather than a flavor name — the original flavor ids (`swiftBoots`, `rapidFire`, `extendedGrace`, etc.) read fine individually but didn't disambiguate from a near-synonym neighbor (is "rapid fire" the cooldown one or the projectile-speed one?), and `extendedGrace` collided with DESIGN.md §7's *unrelated, still-deferred* universal holdable of the same name. `label` is where the actual flavor/effect text lives now, and is deliberately non-numeric (`"이동속도 상승"`, not `"+15%"`) — the mystery-reveal text is the player's only source of truth for what they got, and the exact magnitude is an internal tuning value, not a promise the reveal makes. **Piercing correctness note** (physics-layer, worth knowing): Arcade `overlap` fires every frame two bodies are touching, not once — a piercing shot surviving a hit could otherwise re-trigger `handleProjectileHitEnemy` many times while passing through one big enemy. Fixed via `Projectile.hasHitEnemy`/`recordEnemyHit` (per-projectile hit-once-per-enemy tracking) and `consumePierce()` (only destroys the shot once its pierce count is spent). Reward rolling is hooked into a new `trackRoomCleared()` (split out of `updateDoorVisuals()`, which now stays visual-only) — it's the exact "did this room's enemy list just empty out for the first time" transition `clearedRooms` already needed, guarded to `role === 'host'` so the joiner's own call to the same method never double-rolls. `StateMessage` gained `itemPickups: ItemPickupState[]` (create/destroy-on-presence reconciliation, no interpolation needed — pickups don't move) and `ProjectileState` gained optional `radius` so a size-boosted player's shots render at the right size on the *other* client's screen too. Every pickup, not just Fart, reveals what it actually was on consumption — `showPickupText` (a rising, fading text flash using each item's `getItemLabel`) is the mystery-pickup design's only "what did I just get" feedback, since the ground visual never says. Fart additionally plays a synthesized noise (`playFartSound` — Web Audio, no asset — a short sawtooth blast with a downward pitch bend). Both are triggered locally on whichever client notices the pickup vanish (host's `handleItemPickup`, or the joiner's `reconcileItemPickups` diffing against the previous broadcast), so both players see/hear it regardless of who actually grabbed it. **Same-frame transition bug, fixed**: `updateHostFrame` runs `trackRoomCleared()` (spawns the reward) and `checkRoomTransition()` (checks door/hole-touch) in the same frame — a player already standing inside the boss hole's radius the instant the boss dies (plausible, it's dead center and the boss is a big melee chaser) would immediately trigger `startLevel()`, which tears down `itemPickups` via `loadRoom()` before the reward just spawned that same frame was ever visible. Fixed with a new `roomJustCleared` flag, set only during the exact frame a fresh clear is detected — `checkRoomTransition` holds off for that one frame so a reward always gets at least one visible frame first.
- ✅ Strong-item mechanics expansion (mid-build addition, after room-clear rewards) — `StrongItemId` grew from `multiShot`/`pierce` to include `homing` and `multiDirection`, and every strong-item field on `PlayerStats` (`hasMultiShot`, `hasPiercing`, `hasHoming`, `hasMultiDirection`) changed from boolean to a stacking count (`gameplay/items.ts`) — picking up the same strong item twice compounds it instead of being a no-op. `gameplay/items.ts` also gained a `weight?` field on `ItemDefinition` and `weightedRandomFromRecord`/`randomBoostItemId`/`randomStrongItemId` (a standard weighted-random-pick implementation) so regular-pool odds aren't flat — e.g. `damage` (weight 0.25) and `size` (0.75) are rarer than the 1-weight defaults. Regular room-clear boost drops are now also gated behind a `ROOM_DROP_CHANCE = 0.7` roll in `DevTestScene.ts`'s `rollRoomClearReward` (previously guaranteed), independent of the existing `LIFE_ITEM_CHANCE` heart roll.
  - **Multi Shot** (`hasMultiShot`, capped at 2 stacks): 0 → single shot, 1 → two shots in a symmetric spread, 2+ → three shots (left/center/right). `tryFirePlayer` in `DevTestScene.ts`.
  - **Multi Direction** (`hasMultiDirection`): 0 → fire only toward facing (unchanged default); 1 → facing + its opposite (front/back, still facing-relative); 2 → the four world-cardinal directions; 3+ → eight directions around a full circle. Composes with Multi Shot — each base direction independently expands into 1/2/3 shots. **Note found while documenting, not changed**: tier 1 rotates with the player's facing, but tiers 2 and 3 fire at fixed world angles regardless of facing — looks like it might be a missed `+ facing` offset rather than intentional, but wasn't touched since it wasn't the bug asked about; worth a look before relying on it.
  - **Homing** (`hasHoming`, drives `Projectile.homingStrength`): every host frame, a non-attached homing shot re-targets whichever enemy is currently nearest to *itself* and steers toward it (`Projectile.steerTo`, a gradual velocity-blend, not a snap — stronger with more stacks).
  - **Homing + Pierce "attach"**: a shot with both homing and remaining pierce charges attaches to the first enemy it hits (`Projectile.shouldAttachOnHit`/`attachTo`) instead of continuing to pierce — it sticks to that one enemy and ticks `damage` every 400ms (`shouldTickAttached`) until the target dies, doesn't expire by range while attached, and detaches/self-destroys if its target is removed by something else first.
  - **Bug found and fixed**: the per-frame homing-steer logic ran unconditionally for every homing projectile, attached or not, and always re-targeted "nearest enemy to the projectile" — so an attached shot would drift away from the enemy it was still ticking damage against, and could re-overlap and silently reattach to whatever it drifted into (`shouldAttachOnHit` didn't exclude an already-attached projectile). Fixed two places: the per-frame loop now branches on `isAttached()` first and steers an attached shot toward its own attached enemy's live position instead of the generic nearest-enemy search; `Projectile.shouldAttachOnHit()` itself now also requires `!this.isAttached()`, so a stuck shot can't reassign itself regardless of what else touches it.
  - **Cast cleanup**: `DevTestScene.ts` had accumulated `(projectile as any).method()` + `typeof x === 'function'` runtime checks for every homing/attach call, and a couple of unnecessary `id as any` casts in the dev item-giving menu (`StrongItemId`/`BoostItemId` were already assignable to the params being called). None of it was load-bearing — every method involved is a normal, always-present public method on `Projectile` — so it's all gone now; calls are direct and type-checked again.
  - **Dev debug menu** (new, pause-menu-only, gated behind `import.meta.env.DEV && (role === 'host' || isSolo)`): a button per boost/strong item that calls `giveItemToHost(itemId)`, applying it straight to `hostPlayer` and reusing the same `showPickupText`/`playFartSound` reveal a real pickup gets — for testing item effects without needing to earn the drop.
  - **`sendStateSnapshot()`** (new): a host-only helper that builds and sends one `StateMessage` immediately, outside the normal 50ms broadcast cadence — called from `handleHit` the instant game-over triggers (alongside stopping the broadcast timer and calling `this.physics.pause()`), so the joiner sees the final state right away instead of waiting up to one broadcast interval.
  - **Game-over Space shortcut**: pressing Space on the game-over screen returns to the hub (`DevTestScene.update()`'s new `isGameOver` branch, shared `returnToHub()` extracted from the existing "← 게임 허브" button). Fixed a related latent bug while wiring this up: the joiner's local `isGameOver` field was never actually set from `data.isGameOver` (only `showGameOver()` was called) — the joiner's copy of the flag was always `false`, so the same Space shortcut wouldn't have worked for the joiner. Now mirrored properly, same pattern as `isPaused`.
- ✅ Extracted `GameSimulation` + real single-player `PlayScene` — "let's start building the actual game now": everything role-agnostic that solo mode and co-op's host role had always run (rooms, enemies, projectiles, items, lives, pause, level progression — effectively all of `DevTestScene.ts`'s old `updateHostFrame` and everything it called) moved into a new `src/games/teamGame/simulation/GameSimulation.ts`, constructed with `{ scene, hasJoiner }`. `PlayScene.ts` (new, `TeamGameScenes.Play`) is single-player's real home now — thin, no PeerJS import at all, just drives `GameSimulation` from local input and draws UI (doors/minimap/pause menu/game-over/dev item-menu) from its getters. `DevTestScene.ts` slimmed down to co-op only: `isSolo`/`setupSolo()` are gone; `setupHost` now constructs the same `GameSimulation({ hasJoiner: true })` and just wires PeerJS glue around it (`applyJoinerInput` on a received `InputMessage`, `buildStateMessage()` on the broadcast timer, `togglePause()`/menu-show on `PauseToggleMessage`); `setupJoiner` and all the render-only reconciliation methods are completely untouched — the joiner never simulates, so it has nothing in common with `GameSimulation` and was left alone by design. `LobbyScene.ts`'s solo button now starts `TeamGameScenes.Play` and is relabeled "싱글 플레이" (no longer "solo test (dev)" — it's the real single-player mode). Deliberate boundary, checked against the still-upcoming role system before committing to it (roles extend `GameSimulation`'s existing fire-handling/hit-resolution/item-rolling, not a second split — see the Attack System section's note): `GameSimulation` owns state + logic; it does **not** own PeerJS/connection/broadcast-timer, or any Phaser "chrome" (door/boss-hole graphics, minimap, pause menu, game-over text, dev menu) — those read `GameSimulation`'s getters (`isRoomClear()`, `isCurrentRoomBoss()`, `isDirectionOpen(direction)`, `currentRoomCoord`, `floorRoomEntries`, `exploredRooms`, `currentLevel`, `isGameOver`, `isPaused`) and draw themselves. At this point that presentation code (and the 3 cosmetic effects) was still duplicated between the two scenes — **since folded into shared code, see the next build-status entry.** Two callback hooks (`setOnLevelStart`, `setOnGameOver`) let the co-op scene react to level-start/game-over without `GameSimulation` needing to know networking exists — `PlayScene` just doesn't register either. Alongside this: fixed a real bug found while reviewing the other AI's homing/attach additions (see below) and cleaned up `(projectile as any)` casts that had crept into `DevTestScene.ts` for no reason — every method involved was already a normal typed public method on `Projectile`.
- ✅ Homing/attach correctness fix + cast cleanup — a Homing shot with remaining Pierce charges attaches to the first enemy it hits and ticks damage on a timer (`Projectile.shouldAttachOnHit`/`attachTo`/`shouldTickAttached`) instead of continuing to fly. The per-frame homing-steer logic used to run unconditionally for every homing projectile, attached or not, and always re-targeted "nearest enemy to the projectile itself" — so an attached shot would drift away from the enemy it was still ticking damage against, and could re-overlap and silently reattach to whatever it drifted into. Fixed in two places (now inside `GameSimulation`): the per-frame loop branches on `isAttached()` first and steers an attached shot toward its *own* attached enemy's live position instead of the generic nearest-enemy search; `Projectile.shouldAttachOnHit()` itself now also requires `!this.isAttached()`, so a stuck shot can't reassign itself regardless of what else touches it. Separately, `(projectile as any).method()` + `typeof x === 'function'` runtime checks for every homing/attach call (plus a couple of unnecessary `id as any` casts in the dev item-menu, where `StrongItemId`/`BoostItemId` were already assignable) were removed — none of it was load-bearing, every method involved is a normal always-present public method on `Projectile`.
- ✅ Extracted the remaining shared presentation code — "can I extract as much as possible that will be shared?" as a follow-up to the `GameSimulation` split, which had deliberately left UI-drawing duplicated between `PlayScene.ts`/`DevTestScene.ts` to keep that first pass small. Three new pieces:
  - **`ui/cosmeticEffects.ts`** (new) — `showPickupText`/`spawnExplosionEffect`/`playFartSound` as plain functions (not a class — nothing to construct) instead of duplicated private methods on `GameSimulation` (host) and `DevTestScene` (joiner). Both call sites now just pass `scene` as the first arg.
  - **`RoomUiState`** (new interface, exported from `simulation/GameSimulation.ts`) — the read-only shape a presentation layer needs: `currentRoomCoord`/`currentLevel`/`floorRoomEntries`/`exploredRooms`/`isGameOver`/`isPaused` plus `isCurrentRoomBoss()`/`isRoomClear()`/`isDirectionOpen(direction)`. `GameSimulation` already had exactly this surface, so `implements RoomUiState` was free. The interesting part: `DevTestScene` now `implements RoomUiState` *on itself* too, for the joiner role — `isCurrentRoomBoss`/`isRoomClear`/`isDirectionOpen` became plain methods reading its existing joiner-only fields (`roomEnemies`, `floorRoomEntries`, `currentRoomCoord`), and `currentLevel` (removed as dead in the previous pass) came back, now genuinely read. A small `currentState()` helper picks `this.simulation` for host or `this` for joiner — one call site, not role-branching logic sprinkled through every method like the previous pass's `resolvedIsGameOver`/`resolvedIsPaused`/etc. helpers (all removed, superseded by this).
  - **`ui/GameplayHud.ts`** (new) — the actual shared presenter: door rectangles + boss-hole circle, `MiniMap` + level text, game-over text, and the full pause-menu overlay (incl. dev give-item buttons, `import.meta.env.DEV`-gated inside `GameplayHud` itself now rather than at each call site). Title/instructions/back-to-hub-button text moved in too, parametrized (`title`/`subtitle`/`onReturnToHub`). One `refresh(state: RoomUiState)` call per tick (host/solo) or per received message (joiner) redraws everything; `showPauseMenu()`/`hidePauseMenu()` stay explicit calls at the existing toggle points (Esc, remote `PauseToggleMessage`, received `isPaused`) rather than folded into `refresh()`, matching the previous event-driven timing exactly. `PlayScene.ts` and `DevTestScene.ts` each construct one and are now genuinely thin — both roles in `DevTestScene.ts` share the *same* `GameplayHud` instance, just handing it a different `RoomUiState`. Deliberately still NOT in `GameplayHud`: the co-op host/joiner legend text (doesn't apply to single-player, stays in `DevTestScene.ts`) and the game-over Space-to-hub shortcut (input handling, stays in each scene's own `update()`).
- ⬜ Phase F+ — Role system (data-driven, seven roles, unlocks Laser/Bomb attack shapes), full run structure (including wiring up `respawnForNextLevel`, and role items/holdables once roles exist), shop

## Working conventions

- Before any multi-file change, outline the plan (files touched, key types/interfaces introduced) and wait for confirmation before implementing.
- When touching the networking layer or physics/collision math, flag it explicitly in your summary — these are the areas most worth a careful diff review.
- If a task would require guessing at how the existing main menu, the platformer's code, or an earlier phase's implementation actually works, inspect the relevant files first rather than assuming.
